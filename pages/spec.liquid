---
layout: article
permalink: /spec/index.html
title: JMAP Specifications
description: >-
    The specifications for the core JMAP protocol and access to mail and
    contacts stores using JMAP have been finalised and published.
hero:
    eyebrow: Specs
    title: JMAP RFC
    sub: >-
        The specifications for the core JMAP protocol and access to mail and
        contacts stores using JMAP have been finalised and published. The JMAP 
        working group is currently working on the calendars specs.
    links:
        - href: https://datatracker.ietf.org/wg/jmap/about/
          label: JMAP working group
specs:
    - label: Core
      description: >-
          These specifications define the core sync protocol and extensions
          that 
          are useful no matter what data type you might be syncing.
      items:
          - label: The core JMAP protocol (RFC 8620)
            href: /spec/rfc8620/
          - label: VAPID for JMAP Push (RFC 9749)
            href: /spec/rfc9749/
          - label: JMAP Sharing (RFC 9670)
            href: /spec/rfc9670/
          - label: JMAP via WebSocket (RFC 8887)
            href: /spec/rfc8887/
          - label: JMAP Quotas (RFC 9425)
            href: /spec/rfc9425/
          - label: JMAP Blob Management (RFC 9404)
            href: /spec/rfc9404/
    - label: Mail
      description: >-
          This spec is not yet final and may change before publication. If you 
          find any issues, please email [jmap@ietf.org](mailto:jmap@ietf.org) or
          make a pull request on GitHub if you have a proposed fix.
      items:
          - label: JMAP Mail (RFC 8621)
            href: /spec/rfc8621/
          - label: JMAP Sieve Scripts Management (RFC 9661)]
            href: /spec/rfc9661/
          - label: JMAP MDN Handling (RFC 9007)
            href: /spec/rfc9007/
          - label: JMAP S/MIME Signature Verification (RFC 9219)
            href: /spec/rfc9219/
    - label: Contacts
      items:
          - label: JMAP Contacts (RFC 9610)
            href: /spec/rfc9610/
    - label: Calendars
      items:
          - label: JMAP Calendars (in progress)
            href: /spec/calendars-draft/
    - label: Data formats
      description: >-
          JMAP was designed in conjunction with new JSON-based file formats for 
          contacts and calendars, which can also be used standalone or in other
          protocols.
      items:
          - label: JSContact (RFC9553)
            href: /spec/rfc9553/
          - label: JSCalendar 2.0 (in progress)
            href: /spec/jscalendar-draft/
---
{%- for section in page.specs -%}
    <h2>{{ section.label }}</h2>
    {%- if section.description -%}
        <p>{{ section.description }}</p>
    {%- endif -%}
    <ul>
        {%- for item in section.items -%}
            <li>
                <a href='{{ item.href | relative_url }}'>{{ item.label }}</a>
            </li>
        {% endfor %}
    </ul>
{% endfor %}
